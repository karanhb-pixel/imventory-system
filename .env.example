# Database Configuration for Imventory System
# Copy this file to .env and update the values with your Neon database details

# Neon Database Connection URL
# Get this from your Neon dashboard: https://neon.tech/dashboard
# Format: postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL=your_neon_database_url_here

# Database Environment
# Set to 'production' for live database, 'development' for local dev
NODE_ENV=development

# Optional: Database Pool Settings (for advanced configuration)
DATABASE_POOL_MIN=1
DATABASE_POOL_MAX=10

# For Netlify deployment, set these in Netlify's environment variables section
# The DATABASE_URL will be automatically available when deployed to Netlify